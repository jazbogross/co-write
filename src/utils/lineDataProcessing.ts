
// This file is now just a re-export to maintain backward compatibility
// and avoid breaking existing imports during the refactoring transition

import { processLinesData, processDraftLines, getPlainTextForMapping } from './lineProcessing';

export { processLinesData, processDraftLines, getPlainTextForMapping };
