
import { LineData } from '@/types/lineTypes';

// Placeholder for the Enter-at-Zero strategy
export const handleEnterAtZero = (
  operation: any,
  contents: any[],
  prevData: LineData[],
  usedIndices: Set<number>,
  userId: string | null,
  editor?: any
) => {
  return {
    success: false
  };
};
